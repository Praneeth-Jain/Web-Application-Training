@page "/Login"
@model LoginPage.Pages.SignUpModel
@{
    ViewData["Title"] = "SignUp";
}

<div class="container-fluid">
@* 
    @{
        if (TempData["Message"] != null)
        {
            Console.WriteLine(TempData["Message"]);
            <div>
                <p class="text-light bg-success text-center p-3 sticky-top">@TempData["Message"]</p>
            </div>
        }
    } *@
    
    @{
        if (TempData["LoginError"] != null)
        {
            <p class="bg-danger text-light m-4 text-center">@TempData["LoginError"]</p>
        }
    }
    <form  method="post">
<div class="form-group">
        <label asp-for="User.Username" class="control-label">UserName</label>
        <input type="text" asp-for="User.Username" class="form-control" placeholder="Enter the username" />
        <span asp-validation-for="User.Username" class="bg-danger text-light"></span>
</div>
<div class="form-group"> 
        <label asp-for="User.Password" class="control-label"></label>
        <input type="password" asp-for="User.Password" class="form-control" placeholder="Password" />
        <span asp-validation-for="User.Password" class="bg-danger text-light"></span>
</div>
@* <div class="form-group">
@Html.Label("terms","Terms & Conditions");
@Html.CheckBox("terms",false,new{@class="form-check-control"});
</div> *@
<button class="btn btn-primary mt-4">SignUp</button>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
